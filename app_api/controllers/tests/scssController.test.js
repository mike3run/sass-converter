const expect = require('chai').expect;

describe('scssController', () => {
  it('should repond with error if empty string');
  it('should alert you have syntax error');
  it('should compile scss to sass and return css as well');
});
